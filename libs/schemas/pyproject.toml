[project]
name = "kb-schemas"
version = "0.1.0"
description = "Pydantic domain models, KBDoc contract, API contracts, ORM mapping"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.9",
    "sqlalchemy>=2.0.32",
    "pgvector>=0.3.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/kb_schemas"]
